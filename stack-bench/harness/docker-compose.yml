version: "3.8"
services:
  postgres:
    image: postgres:16-alpine
    container_name: stackbench-pg
    environment:
      POSTGRES_USER: bench
      POSTGRES_PASSWORD: bench
      POSTGRES_DB: bench
    ports:
      - "5433:5432"
